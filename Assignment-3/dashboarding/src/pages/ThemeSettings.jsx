import React from 'react';
import { Typography } from '@mui/material';

const ThemeSettings = () => <Typography variant="h5">Toggle the theme from the top bar.</Typography>;

export default ThemeSettings;
