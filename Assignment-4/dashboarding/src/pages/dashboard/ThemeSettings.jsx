import React from 'react';
import { Typography } from '@mui/material';

const ThemeSettings = () => <Typography variant="h5">Toggle the theme from  theme SideBar.</Typography>;

export default ThemeSettings;
